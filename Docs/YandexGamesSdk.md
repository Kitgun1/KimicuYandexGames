# ⚙️ YandexGamesSdk
## Инициализация
```csharp
// ...
yield return Initialize(onSuccessCallback = null);
// ...
```
> onSuccessCallback - вызывается по завершению инициализации

## Fields
```csharp
/// Используйте его, чтобы проверить, используете ли вы сборку и запуск или работаете в редакторе.
/// Можно вызвать без инициализации SDK, можно вызвать в редакторе.
YandexGamesSdk.IsRunningOnYandex; // bool

// ============================================================================= //

/// Включите его для регистрации обратных вызовов SDK в консоли.
YandexGamesSdk.CallbackLogging; // bool

// ============================================================================= //

/// Используйте его, чтобы проверить, инициализирован ли SDK.
YandexGamesSdk.IsInitialized; // bool

// ============================================================================= //

/// Данные (id app, lang, и тд)
YandexGamesSdk.Environment; // YandexGamesEnvironment

// ============================================================================= //

/// Cерверное время в миллисекундах, одинаковое на всех устройствах. 
/// Он похож на Date.now() форматом результата, однако последний возвращает время устройства пользователя, 
/// которое может отличаться от серверного и не защищено от накруток со стороны игроков
YandexGamesSdk.ServerTime; // string. Например, 1720613073778.
```

## Methods
```csharp
/// Вызывает GameReady в начале игры. 
/// ВАЖНО: Вызывайте этот метод в момент готовности игры, когда игрок может куда-то кликать.
YandexGamesSdk.GameReady(); // void

/// Вызывает GameStart. 
/// ВАЖНО: Вызывайте этот метод, когда игрок начинает или возобновляет игровой процесс.
/// Например, запуск уровня, закрытие меню, снятие с паузы, возобновление игры после показа рекламы, возвращение в текущую вкладку браузера.
YandexGamesSdk.GameStart(); // void

/// Вызывает GameStop. 
/// ВАЖНО: Вызывайте этот метод, когда игрок приостанавливает или завершает игровой процесс.
/// Например, прохождение уровня или проигрышб, вызов меню, пауза в игре, показ полноэкранной или rewarded-рекламы, уход в другую вкладку браузера.
YandexGamesSdk.GameStop(); // void
```

## Classes
```csharp
class YandexGamesEnvironment {
    App app; { id }
    Browser browser; { lang }
    Internationalization i18n; { lang; tld }
    string payload;
}
```